{
  "name": "holesail-gui",
  "version": "0.12.8",
  "description": "Desktop GUI for Holesail — share local ports and connect to P2P tunnels",
  "author": "chethan62",
  "license": "MIT",
  "scripts": {
    "predev": "node scripts/prepare-resources.mjs",
    "dev": "tauri dev",
    "lint": "eslint renderer/ service-worker.js worker/ test/ scripts/",
    "lint:fix": "eslint renderer/ service-worker.js worker/ test/ scripts/ --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build": "node scripts/prepare-resources.mjs --bare && tauri build",
    "build:no-bundle": "node scripts/prepare-resources.mjs --bare && tauri build --no-bundle",
    "prepare:android": "node scripts/prepare-resources.mjs --bare --target android-arm64 --out dist-resources-android",
    "build:android": "npm run prepare:android && node scripts/android-glue.mjs && tauri android build --apk --debug",
    "build:android:release": "npm run prepare:android && node scripts/android-glue.mjs && tauri android build --apk",
    "test": "node test/service.test.js && node test/renderer.test.js && node test/units.test.js && node test/fileserver.test.js && node test/invite.test.js && node test/session-state.test.js",
    "gate": "npm run format:check && npm run lint && npm test && npm run test:bare && node scripts/prepare-resources.mjs --bare --target linux-x64 && cargo fmt --manifest-path src-tauri/Cargo.toml --check && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings && cargo test --manifest-path src-tauri/Cargo.toml",
    "test:bare": "node scripts/prepare-resources.mjs --bare --target linux-x64 --out dist-resources-bare && WORKER_CMD=dist-resources-bare/bare WORKER_PATH=dist-resources-bare/service-worker.js node test/service.test.js && node test/renderer.test.js",
    "tauri": "tauri"
  },
  "dependencies": {
    "holesail": "^2.4.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "bare-http1": "^4.6.2",
    "eslint": "^10.8.1",
    "prettier": "^3.9.6"
  }
}
