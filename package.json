{
  "name": "holesail-gui",
  "version": "0.1.0",
  "description": "Desktop GUI for Holesail — share local ports and connect to P2P tunnels",
  "main": "main.js",
  "author": "Reasonix",
  "license": "MIT",
  "scripts": {
    "dev": "tauri dev",
    "build": "node scripts/prepare-resources.mjs && tauri build",
    "build:no-bundle": "node scripts/prepare-resources.mjs && tauri build --no-bundle",
    "test": "node test/service.test.js",
    "tauri": "tauri"
  },
  "dependencies": {
    "holesail": "^2.4.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
